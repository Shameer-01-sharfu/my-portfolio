const root=document.documentElement;const saved=localStorage.getItem('theme');if(saved)root.dataset.theme=saved;const toggle=document.querySelector('[data-theme-toggle]');function updateThemeLabel(){if(!toggle)return;const dark=root.dataset.theme==='dark';toggle.textContent=dark?'☀ Light':'☾ Dark';toggle.setAttribute('aria-label',dark?'Switch to light mode':'Switch to dark mode');}updateThemeLabel();toggle?.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark';if(next==='dark')root.dataset.theme='dark';else delete root.dataset.theme;localStorage.setItem('theme',next);updateThemeLabel();});const navToggle=document.querySelector('.nav-toggle'),nav=document.querySelector('#primary-navigation');navToggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});document.querySelectorAll('#primary-navigation a').forEach(a=>a.addEventListener('click',()=>{nav?.classList.remove('open');navToggle?.setAttribute('aria-expanded','false');}));const form=document.querySelector('#contact-form');form?.addEventListener('submit',e=>{e.preventDefault();let ok=true;form.querySelectorAll('[required]').forEach(el=>{const err=document.querySelector(`#${el.id}-error`);if(!el.checkValidity()){ok=false;if(err)err.textContent=el.validationMessage;}else if(err)err.textContent='';});const status=document.querySelector('#form-status');if(ok){status.textContent='Thanks! Your message is validated locally. Connect this form to your preferred email service before production use.';form.reset();}});document.querySelector('[data-year]')?.replaceChildren(String(new Date().getFullYear()));
